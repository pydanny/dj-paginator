============
Installation
============

At the command line::

    $ easy_install dj-paginator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-paginator
    $ pip install dj-paginator
